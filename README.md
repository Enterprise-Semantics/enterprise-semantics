# enterprise-semantics
Canonical enterprise-semantics assets: authoritative structured semantic source (YAML/JSON concepts, relationships, identifier registry). The single source of truth for enterprise-level semantics within Enterprise-Semantics.
